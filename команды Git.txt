list - ls -- список файлов в текущей активной директории
	ls -a -- показ всех файлов(квлючая скрытые)
cd (..) -- переход в директорию
	cd .. -- переход на уровень назад
make directori - mkdir (..) -- создать директорию
touch (..) -- создать файл
copy - cp (..) (..) -- копировать файл
move - mv (..) (..) -- переименовать
echo ".." -- записать строку
	echo ".." > (..) -- записат в файл
cat (..) -- открыть файл
remove - rm (..) -- удалить файл
	rm -R -- рекурсивное удаление директории
ssh-keygen -- создать ключ для подключения репозитория

git init -- создать пустой репозиторий
git status -- статус репозитория
git remote origin (..) -- связать локальный и удаленный репозитории
	git remote -v -- список подключенных соединений
git add (..) -- зарегистрировать отслеживаемый файл
	git add . -- регистрация всех новых файлов
git rm --cached (..) -- удалить отслеживаемый файл
git restore (..) -- отменить изменение до предыдущего (коммита)
git commit -- добавить коммит
	git commit -m(..) -- записать (коммит) с сообщением
	git commit -a -- автоматическая регистрация созданного комита
	git commit -am -- автоматическая запись коммита с сообщением
	git commit --amend -- внести изменения в последний коммит
git log -- история коммитов
	git log --oneline -- компактная история коммитов
git reset (..) -- удалить (коммит)
	git reset --hard -- откат до предыдущего коммита
git reflog -- история операций
git revert (..) -- отменить (коммит)
git restore -- сброс файла на указанное состояние
git branch -- текущие ветки репозитория
	git branch (..) -- создать ветку
	git branch -d (..) -- удалить ветку
git checkout (..) -- переключится на (ветку)
	git checkout -b (..) -- создать и переключится на ветку
git clone <..> (..) -- клонировать <репозиторий> в (новую папку)
git push -- загрузить копию репозитория на сервер
	git push -m origin (..) -- загрузить копию репозитория с созданием новой ветки
	git push --delete origin (..) -- удалить (что-то) с сервера
	git push --force -- принудительная перезапись удаленного репозитория
	git push --tags -- загруска тегов в удаленный репозиторий
	git push -u origin (..) -- загрузить и создать ветку в удаленном репозитории
	git push --force -- перезапись истории на удаленном репозитории
git pull -- загрузить измененния репозитория с сервера
git fetch -- обновить синхронизацию с сервером
git merge (..) -- произвести слияние (ветки) с текущей веткой
	git merge squash-dev  -- squash -- объединить коммиты
git rebase -- изменение места старта ветки
git tag (..) -- задать (версию)
	git tag --list -- просмотр всех версий
	git tag -d (..) -- удалить тег
git stash -- сохранить изменения на временное хранилище stash
	git stash list - список stash
	git stash push (..) -- добавить новый файл в текущий stash
	git stah pop (..) -- добавить изменения на ветку
git rebase -i HEAD~X -- объединить Х коммитов
git cherry-pik <commit> -- перенести коммит на текущую ветку
git cherry-pik <branch> -- перенести последний коммит ветки
git sherry-pik ..<branch> -- перенести все коммиты ветки



.gitkeep -- для пустых папок
.gitignore -- файлы только для локальных репозиториев